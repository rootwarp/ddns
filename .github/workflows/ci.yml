name: CI

on:
  push:
  pull_request:
    branches:
      - develop
      - main

jobs:
  test:
    name: test
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        go: ["1.26.x"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/Library/Caches/go-build
          key: ${{ runner.os }}-go-${{ matrix.go }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-${{ matrix.go }}-

      - name: Install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@latest

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

      - name: Lint
        run: make lint

      - name: Test
        run: make test

      - name: Build
        run: make build

      - name: Smoke — version
        shell: bash
        run: |
          out=$(./bin/ddns version)
          echo "$out"
          echo "$out" | grep -E '^ddns [^ ]+ \(commit [^,]+, built .+\)$'

      - name: Smoke — help
        run: ./bin/ddns --help

      - name: Smoke — run SIGTERM exits cleanly
        shell: bash
        # Phase 2 makes `ddns run` require a --config and a reachable DNS
        # provider. DDNS_FAKE_PROVIDER=fake is a test-only escape hatch in
        # cmd/ddns/run.go that swaps in the in-memory provider so the smoke
        # step stays green on runners with no Google ADC.
        env:
          DDNS_FAKE_PROVIDER: fake
        run: |
          ./bin/ddns run --config cmd/ddns/testdata/example.yaml &
          PID=$!
          sleep 2
          kill -TERM "$PID"
          wait "$PID"
          STATUS=$?
          echo "ddns run exit status: $STATUS"
          # 0  = handler caught SIGTERM and returned nil (expected).
          # 143 = 128+15, external SIGTERM before handler registered — tolerated
          # only so a slow-boot CI runner doesn't flake.
          if [ "$STATUS" -ne 0 ] && [ "$STATUS" -ne 143 ]; then
            echo "Unexpected exit: $STATUS"
            exit 1
          fi

  docker:
    name: docker
    # Linux-only: macOS GitHub-hosted runners do not ship a working Docker
    # daemon by default, and Phase 5 deployment shape is Linux containers.
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build image
        run: |
          docker build \
            -t ddns:ci \
            -f deploy/docker/Dockerfile \
            --build-arg VERSION=ci \
            --build-arg COMMIT=${GITHUB_SHA::7} \
            --build-arg BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ) \
            .

      - name: Smoke — container version
        shell: bash
        run: |
          out=$(docker run --rm ddns:ci version)
          echo "$out"
          # Must start with "ddns " (distinguishes real version output from
          # urfave/cli help text or an unexpected panic).
          echo "$out" | grep -E '^ddns '

      - name: Report image size
        # Informational: Phase 5 targets <30 MB. CI does not fail the job
        # on size alone — image size is tracked in the release-gate runbook.
        run: docker images ddns:ci --format 'ddns:ci size: {{.Size}}'
